@{
    Layout = "_adminLayout.cshtml";
    IQueryable<ProductModel> result = Model;
}


<div class="d-flex justify-content-between align-items-center mb-3">
    <h4>Ürün Yönetimi</h4>
    <a href="AddProduct" class="btn btn-sm btn-primary">
        <i class="fa-solid fa-plus"></i> Ürün Ekle
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body p-0">
         @if(result is not null){
        <table class="table table-striped mb-0 align-middle">
            <thead class="table-light">
                <tr>
                    <th>Başlık</th>
                    <th>İçerik</th>
                    <th>Oluşturulma Tarihi</th>
                    <th class="text-end">Güncelle</th>
                </tr>
            </thead>
            <tbody>
                     @foreach(var entity in result)
                {
                    <tr>
                        <td> @entity.ProductTitle </td>
                        <td> @entity.ProductTitle </td>
                        <td> @entity.CreatedDate </td>
                          <td class="text-end">
                        <a href="@Url.ActionLink("SliderEdit", "Admin", new { id = entity.Id })" class="btn btn-warning">
                            <i class="fa-solid fa-pen"></i>
                        </a>
                    </td>
                    </tr>
                }   
               
            </tbody>
        </table>
        }
         else{
              <h5 class=" d-flex justify-content-center card-title">Veri Bulunamadı</h5>
            }
    </div>
</div>
